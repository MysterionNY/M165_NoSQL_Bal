# MongoDB – Vom Relationalen Denken zum Document-Design

# Inhaltsverzeichnis

# Phase 1: Grundlagen

## MongoDB Installation auf AWS EC2

### Verwendete AWS-Konfiguration

| Einstellung | Wert |
|---|---|
| Betriebssystem | Ubuntu Server 26.04 |
| Instanztyp | t2.micro |
| Speicherplatz | 20 GB |
| Statische IP | Elastic IP verwendet |
| Datenbank-Port | 27017 |
| SSH-Port | 22 |

### Security Group Konfiguration

Folgende Ports wurden geöffnet:

| Typ | Protokoll | Port | Zweck |
|---|---|---|---|
| SSH | TCP | 22 | Remote-Zugriff per SSH |
| Custom TCP | TCP | 27017 | MongoDB Verbindung |

### Cloud-Init Konfiguration

Beim Erstellen der EC2-Instanz wurde das bereitgestellte Cloud-Init-Skript im Feld **User Data** eingefügt.

Das Skript übernimmt automatisch:

- Installation von MongoDB 6.0
- Aktivierung des MongoDB Services
- Öffnen der Datenbank für externe Verbindungen
- Aktivierung der Authentifizierung
- Erstellung eines Admin-Benutzers
- Starten des MongoDB Dienstes

Das Standardpasswort wurde angepasst.

![Connection](/Kompetenznachweise/KN02/Images/phase1-connection.png)

![DB Login](/Kompetenznachweise/KN02/Images/phase1-dblogin.png)

![Output](/Kompetenznachweise/KN02/Images/phase1-output.png)

### Modellierung

MongoDB ist eine dokumentenorientierte NoSQL-Datenbank und unterscheidet sich stark von relationalen SQL-Datenbanken.  
Während relationale Datenbanken Daten häufig stark normalisieren und auf mehrere Tabellen verteilen, versucht MongoDB Daten möglichst zusammenhängend in einem Dokument zu speichern.

Das Ziel in MongoDB ist sogenannte **Data Locality**.  
Daten, die häufig gemeinsam gelesen werden, sollen auch gemeinsam gespeichert werden. Dadurch können komplexe Joins und mehrere Abfragen vermieden werden.

#### Referenzielle Integrität

Im Gegensatz zu relationalen Datenbanken überprüft MongoDB Fremdschlüsselbeziehungen nicht automatisch.  
Die Applikation selbst ist dafür verantwortlich, dass Beziehungen zwischen Dokumenten korrekt bleiben.

Dadurch kann beispielsweise ein Dokument auf eine ID verweisen, die gar nicht existiert.

#### Normalisierung und Redundanzen

In relationalen Datenbanken wird oft stark normalisiert, um doppelte Daten zu vermeiden.  
MongoDB verfolgt häufig den gegenteiligen Ansatz und erlaubt bewusst Redundanzen.

Dadurch können Daten schneller gelesen werden, da weniger Verknüpfungen notwendig sind.

Beispielsweise kann eine Adresse direkt in einem Dokument gespeichert werden, anstatt dafür eine separate Collection zu verwenden.

### Modellierungstechniken in MongoDB

MongoDB bietet verschiedene Möglichkeiten Beziehungen abzubilden.

##### Embedding

Beim Embedding werden zusammengehörige Daten direkt in ein Dokument eingebettet.

Beispiel:

```json
{
  "title": "Spaghetti Carbonara",
  "ingredients": [
    {
      "name": "Spaghetti",
      "amount": 400,
      "unit": "g"
    }
  ]
}
```

Vorteil:

* Schnelleres Lesen
* Weniger Abfragen notwendig
* Bessere Performance

Nachteil:

* Teilweise Redundanzen

##### Referenzen

Bei Referenzen werden Beziehungen ähnlich wie in SQL über IDs hergestellt.

Beispiel:

```json
{
  "recipe_id": "123",
  "ingredient_id": "456"
}
```

Vorteil:

* Weniger doppelte Daten

Nachteil:

* Zusätzliche Lookups notwendig
* Schlechtere Performance bei vielen Verknüpfungen

### Fazit

MongoDB bevorzugt dokumentenorientierte Datenmodelle mit Embedding und Denormalisierung.
Dadurch können Daten schneller gelesen werden und die Anzahl komplexer Joins wird reduziert.

Für Anwendungen mit vielen Lesezugriffen eignet sich dieses Vorgehen besonders gut.

## MongoDB Übersicht

MongoDB ist eine dokumentenorientierte NoSQL-Datenbank.  
Die Daten werden nicht in Tabellen gespeichert, sondern in sogenannten Collections mit JSON-ähnlichen Dokumenten.

Jedes Dokument besitzt automatisch ein Feld namens `_id`, welches als Primärschlüssel dient.  
Falls kein `_id` Feld angegeben wird, erstellt MongoDB dieses automatisch.

### Verbindung mit MongoDB

Die Verbindung zur MongoDB kann über die Mongo Shell (`mongosh`) erfolgen.

Beispiel:

```bash
mongosh "mongodb://<IP-ADRESSE>:27017/"
````

Mit Benutzername:

```bash
mongosh -u admin
```

### Administration

#### Datenbanken anzeigen

```bash
show dbs
```

### Datenbank wechseln

```bash
use rezeptplattform
```

#### Collections anzeigen

```bash
show collections
```

#### Collection erstellen

```js
db.createCollection("recipes")
```

#### Collection löschen

```js
db.recipes.drop()
```

### CRUD Operationen

CRUD steht für:

* Create
* Read
* Update
* Delete

### Read (Lesen)

Dokumente können mit `find()` abgefragt werden.

Beispiel:

```js
db.recipes.find()
```

Mit Filter:

```js
db.recipes.find({ difficulty: "Easy" })
```

### Insert (Einfügen)

Ein einzelnes Dokument hinzufügen:

```js
db.recipes.insertOne({
  "title": "Spaghetti Carbonara",
  "difficulty": "Medium",
  "prep_time_min": 25
})
```

Mehrere Dokumente hinzufügen:

```js
db.recipes.insertMany([
  { "title": "Carbonara" },
  { "title": "Pfannkuchen" }
])
```

### Update (Aktualisieren)

Ein Dokument aktualisieren:

```js
db.recipes.updateOne(
  { title: "Carbonara" },
  {
    $set: {
      difficulty: "Easy"
    }
  }
)
```

### Delete (Löschen)

Ein Dokument löschen:

```js
db.recipes.deleteOne({
  title: "Carbonara"
})
```

### Aggregationen

Mit Aggregationen können komplexe Abfragen erstellt werden.
MongoDB verwendet dafür sogenannte Pipelines.

Eine Pipeline besteht aus mehreren Stages, welche nacheinander ausgeführt werden.

Beispiel:

```js
db.orders.aggregate([
  {
    $match: {
      size: "medium"
    }
  },
  {
    $group: {
      _id: "$name",
      totalQuantity: {
        $sum: "$quantity"
      }
    }
  }
])
```

### Wichtige Aggregation-Stages

| SQL      | MongoDB  |
| -------- | -------- |
| WHERE    | $match   |
| GROUP BY | $group   |
| SELECT   | $project |
| ORDER BY | $sort    |
| LIMIT    | $limit   |
| JOIN     | $lookup  |

## Lookups (Joins)

MongoDB unterstützt Joins über den Aggregation-Stage `$lookup`.

Damit können Daten aus mehreren Collections zusammengeführt werden.

Beispiel:

```js
{
  $lookup: {
    from: "ingredients",
    localField: "ingredient_id",
    foreignField: "_id",
    as: "ingredient"
  }
}
```

Der `$lookup` verbindet dabei Dokumente anhand gemeinsamer IDs ähnlich wie ein SQL Join.

### Fazit

MongoDB bietet flexible Möglichkeiten zur Speicherung und Verarbeitung von Daten.
Neben einfachen CRUD-Operationen ermöglichen Aggregationen und Lookups auch komplexe Datenabfragen über mehrere Collections hinweg.

## MongoDB Skalierung

### Skalierung in MongoDB

MongoDB wurde von Anfang an als verteiltes System entwickelt.  
Die Datenbank besitzt bereits im Kern Mechanismen, um Daten automatisch auf mehrere Server zu verteilen.

Dadurch kann MongoDB grosse Datenmengen und hohe Last besser skalieren als viele klassische relationale Datenbanken.

### Data Locality

In MongoDB gilt das Prinzip der sogenannten **Data Locality**.

Das bedeutet:

> Daten, die häufig gemeinsam verwendet werden, sollen auch gemeinsam gespeichert werden.

Dadurch müssen weniger Daten über das Netzwerk zwischen verschiedenen Servern übertragen werden.

### Denormalisierung

MongoDB verwendet häufig den Ansatz der Denormalisierung.

Dabei werden Daten bewusst mehrfach gespeichert, um schnelle Lesezugriffe zu ermöglichen.

Beispiel:

Bei einer Bestellung können Benutzerinformationen und Produktpreise direkt in der Bestellung gespeichert werden.

Vorteile:

- Schnellere Abfragen
- Weniger komplexe Joins
- Bessere Skalierbarkeit

Nachteil:

- Redundante Daten
- Höherer Speicherverbrauch

### JOINS und $lookup

In relationalen Datenbanken werden Daten häufig über Joins zusammengeführt.

Verteilte Joins sind jedoch sehr langsam, da Daten zwischen mehreren Servern ausgetauscht werden müssen.

MongoDB versucht deshalb, klassische Joins möglichst zu vermeiden.

Der Aggregation-Stage `$lookup` existiert zwar, gilt jedoch eher als Ausnahme oder „Notlösung“ und nicht als Standardvorgehen.

### Warum MongoDB gut skaliert

MongoDB skaliert gut, weil:

- Daten lokal gespeichert werden
- weniger Joins notwendig sind
- Daten bereits vollständig in einem Dokument vorliegen
- Netzwerkkommunikation reduziert wird

Dadurch können Abfragen deutlich effizienter ausgeführt werden.

### Replikation und Sharding

MongoDB unterstützt verschiedene Skalierungstechniken.

#### Replikation

Bei der Replikation werden Daten auf mehrere Server kopiert.

Vorteile:

- Höhere Ausfallsicherheit
- Backup bei Serverausfällen
- Bessere Verfügbarkeit


#### Sharding

Beim Sharding werden Daten auf mehrere Server aufgeteilt.

Dadurch können:

- grössere Datenmengen verarbeitet werden
- Lasten verteilt werden
- Abfragen parallel ausgeführt werden

### Fazit

MongoDB wurde speziell für horizontale Skalierung entwickelt.  
Durch Denormalisierung, Data Locality und die Vermeidung komplexer Joins kann MongoDB grosse Datenmengen effizient verarbeiten und über mehrere Server skalieren.

# Phase 2: Das relationale Erbe

## Ziel

In dieser Phase wurde eine Aggregation-Pipeline erstellt, welche mehrere normalisierte Collections miteinander verbindet.

Das Ziel war es, ein vollständiges Rezept inklusive aller Zutaten und Metadaten auszugeben.

## Verwendete Collections

Für das relationale Modell wurden drei Collections verwendet:

- recipes
- ingredients
- recipe_ingredients

Die Beziehungen zwischen Rezepten und Zutaten wurden über Referenzen mit ObjectIds umgesetzt.

## Importierte Testdaten

Die vorgegebenen JSON-Dummy-Daten wurden in MongoDB importiert.

Dabei wurden:

- Rezepte
- Zutaten
- Verknüpfungen zwischen Rezepten und Zutaten

in separaten Collections gespeichert.

## Aggregation-Pipeline

```js
db.recipes.aggregate([
  {
    $match: {
      _id: ObjectId("651c00000000000000000201")
    }
  },
  {
    $lookup: {
      from: "recipe_ingredients",
      localField: "_id",
      foreignField: "recipe_id",
      as: "recipe_ingredients"
    }
  },
  {
    $unwind: "$recipe_ingredients"
  },
  {
    $lookup: {
      from: "ingredients",
      localField: "recipe_ingredients.ingredient_id",
      foreignField: "_id",
      as: "ingredient"
    }
  },
  {
    $unwind: "$ingredient"
  },
  {
    $group: {
      _id: "$_id",
      title: { $first: "$title" },
      difficulty: { $first: "$difficulty" },
      prep_time_min: { $first: "$prep_time_min" },
      ingredients: {
        $push: {
          name: "$ingredient.name",
          category: "$ingredient.category",
          amount: "$recipe_ingredients.amount",
          unit: "$recipe_ingredients.unit"
        }
      }
    }
  }
])
```

## Erklärung

Die Pipeline startet in der Collection `recipes` und filtert zuerst das gewünschte Rezept heraus.

Mit `$lookup` werden anschliessend die passenden Dokumente aus der Collection `recipe_ingredients` geladen.

Danach wird mit einem zweiten `$lookup` der echte Zutatenname aus der Collection `ingredients` hinzugefügt.

Durch `$group` werden alle Daten wieder zu einem vollständigen Rezept mit einem Zutaten-Array zusammengeführt.

## Ergebnis / Beweis

Die Aggregation-Pipeline liefert ein vollständiges Rezept inklusive aller Zutaten und Metadaten zurück.

![Aggregation Ergebnis](/Kompetenznachweise/KN02/Images/phase2-aggregation.png)

# Phase 3: Die Perfomance-Analyse

## Explain Analyse

Für die Analyse wurde die Aggregation-Pipeline mit `explain("executionStats")` ausgeführt.

```js
db.recipes.explain("executionStats").aggregate([
  {
    $match: {
      _id: ObjectId("651c00000000000000000201")
    }
  },
  {
    $lookup: {
      from: "recipe_ingredients",
      localField: "_id",
      foreignField: "recipe_id",
      as: "recipe_ingredients"
    }
  },
  {
    $unwind: "$recipe_ingredients"
  },
  {
    $lookup: {
      from: "ingredients",
      localField: "recipe_ingredients.ingredient_id",
      foreignField: "_id",
      as: "ingredient"
    }
  },
  {
    $unwind: "$ingredient"
  },
  {
    $group: {
      _id: "$_id",
      title: { $first: "$title" },
      difficulty: { $first: "$difficulty" },
      prep_time_min: { $first: "$prep_time_min" },
      ingredients: {
        $push: {
          name: "$ingredient.name",
          category: "$ingredient.category",
          amount: "$recipe_ingredients.amount",
          unit: "$recipe_ingredients.unit"
        }
      }
    }
  }
])
```

## Metriken

| Metrik              | Wert  |
| ------------------- | ----- |
| totalDocsExamined   | 1     |
| nReturned           | 1     |
| executionTimeMillis | 46 ms |

# Beweis

Hier wird ein Screenshot des `explain("executionStats")` Outputs eingefügt.

![Output](/Kompetenznachweise/KN02/Images/phase3-output.png)

## Fazit

Die Aggregation-Pipeline funktioniert korrekt, benötigt jedoch mehrere `$lookup`-Operationen zwischen verschiedenen Collections.

Dadurch muss MongoDB zusätzliche Dokumente durchsuchen und Daten mehrfach zusammenführen.

Obwohl nur wenige Daten vorhanden sind, mussten bereits mehrere Dokumente untersucht werden. Bei grossen Datenmengen würde dieses relationale Design deutlich schlechter skalieren.

Das zeigt, warum stark normalisierte SQL-Modelle in MongoDB problematisch sein können und weshalb MongoDB eher auf Embedding und Denormalisierung setzt.

# Phase 4 – Der Paradigmenwechsel

## Ziel

In dieser Phase wurde das bisherige relationale Datenmodell überarbeitet.  
Statt die Daten auf mehrere Collections zu verteilen, werden die Rezeptdaten nun in einer einzelnen Collection gespeichert.

Das Ziel ist es, zusammengehörige Daten gemeinsam abzulegen, damit vollständige Rezepte ohne `$lookup` abgefragt werden können.

## Neues Datenmodell

Die neue Collection heisst:

```js
recipes_embedded
```

Ein Rezept wird nun als vollständiges Dokument gespeichert.

```json
{
  "_id": "651c00000000000000000201",
  "title": "Spaghetti Carbonara",
  "difficulty": "Medium",
  "prep_time_min": 25,
  "ingredients": [
    {
      "ingredient_id": "651c00000000000000000001",
      "name": "Spaghetti",
      "category": "Pasta",
      "amount": 400,
      "unit": "g"
    },
    {
      "ingredient_id": "651c00000000000000000002",
      "name": "Guanciale (Speck)",
      "category": "Meat",
      "amount": 150,
      "unit": "g"
    },
    {
      "ingredient_id": "651c00000000000000000003",
      "name": "Eier",
      "category": "Dairy",
      "amount": 4,
      "unit": "Stück"
    }
  ]
}
```
## Begründung

Beim neuen Datenmodell wurden die Zutaten direkt in das Rezept eingebettet.
Das bedeutet, dass die Informationen aus `recipe_ingredients` und `ingredients` nicht mehr über separate Collections zusammengesucht werden müssen.

Diese Entscheidung passt gut zu MongoDB, weil ein Rezept normalerweise gemeinsam mit seinen Zutaten angezeigt wird. Die Daten werden also auch gemeinsam gelesen. Dadurch wird das Prinzip der **Data Locality** eingehalten.

Zwar entstehen durch das Speichern von Zutatennamen und Kategorien gewisse Redundanzen, diese sind in diesem Fall aber akzeptabel. Rezepte werden deutlich häufiger gelesen als verändert. Deshalb ist das Read-to-Write Ratio hoch.

Ausserdem handelt es sich bei Rezepten und Zutaten meistens um eine überschaubare 1-to-Few-Beziehung. Ein Rezept besitzt normalerweise nur eine begrenzte Anzahl an Zutaten. Deshalb eignet sich ein eingebettetes Array sehr gut.

Durch dieses Re-Design kann ein vollständiges Rezept später mit einer einfachen `find()`-Abfrage geladen werden, ohne komplexe `$lookup`-Operationen.

# Phase 5 – Der Beweis

## Neue Collection erstellen

In `mongosh`:

```js
db.createCollection("recipes_embedded")
```

## Drei vollständige Datensätze einfügen

```js
db.recipes_embedded.insertMany([
  {
    "_id": ObjectId("651c00000000000000000201"),
    "title": "Spaghetti Carbonara",
    "difficulty": "Medium",
    "prep_time_min": 25,
    "ingredients": [
      {
        "ingredient_id": ObjectId("651c00000000000000000001"),
        "name": "Spaghetti",
        "category": "Pasta",
        "amount": 400,
        "unit": "g"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000002"),
        "name": "Guanciale (Speck)",
        "category": "Meat",
        "amount": 150,
        "unit": "g"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000003"),
        "name": "Eier",
        "category": "Dairy",
        "amount": 4,
        "unit": "Stück"
      }
    ]
  },
  {
    "_id": ObjectId("651c00000000000000000202"),
    "title": "Klassischer Pfannkuchen",
    "difficulty": "Easy",
    "prep_time_min": 15,
    "ingredients": [
      {
        "ingredient_id": ObjectId("651c00000000000000000004"),
        "name": "Mehl",
        "category": "Baking",
        "amount": 200,
        "unit": "g"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000005"),
        "name": "Milch",
        "category": "Dairy",
        "amount": 300,
        "unit": "ml"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000003"),
        "name": "Eier",
        "category": "Dairy",
        "amount": 2,
        "unit": "Stück"
      }
    ]
  },
  {
    "_id": ObjectId("651c00000000000000000203"),
    "title": "Tomaten Pasta",
    "difficulty": "Easy",
    "prep_time_min": 20,
    "ingredients": [
      {
        "ingredient_id": ObjectId("651c00000000000000000001"),
        "name": "Spaghetti",
        "category": "Pasta",
        "amount": 250,
        "unit": "g"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000006"),
        "name": "Tomaten",
        "category": "Vegetables",
        "amount": 300,
        "unit": "g"
      }
    ]
  }
])
```

## Einfache find-Abfrage

```js
db.recipes_embedded.find({
  _id: ObjectId("651c00000000000000000201")
})
```

## Explain ausführen

```js
db.recipes_embedded.find({
  _id: ObjectId("651c00000000000000000201")
}).explain("executionStats")
```
## Ziel

In dieser Phase wurde das neue eingebettete Datenmodell getestet.  
Das Ziel war es zu beweisen, dass ein vollständiges Rezept nun ohne `$lookup` abgefragt werden kann.

## Migration

Die Daten wurden manuell in die neue Collection `recipes_embedded` eingefügt.  
Dabei wurden vollständige Rezept-Dokumente erstellt, welche die Zutaten direkt als eingebettetes Array enthalten.

```js
db.recipes_embedded.insertMany([
  {
    "_id": ObjectId("651c00000000000000000201"),
    "title": "Spaghetti Carbonara",
    "difficulty": "Medium",
    "prep_time_min": 25,
    "ingredients": [
      {
        "ingredient_id": ObjectId("651c00000000000000000001"),
        "name": "Spaghetti",
        "category": "Pasta",
        "amount": 400,
        "unit": "g"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000002"),
        "name": "Guanciale (Speck)",
        "category": "Meat",
        "amount": 150,
        "unit": "g"
      },
      {
        "ingredient_id": ObjectId("651c00000000000000000003"),
        "name": "Eier",
        "category": "Dairy",
        "amount": 4,
        "unit": "Stück"
      }
    ]
  }
])
```

## Query

Das vollständige Rezept kann nun mit einer einfachen `find()`-Abfrage geladen werden.

```js
db.recipes_embedded.find({
  _id: ObjectId("651c00000000000000000201")
})
```

## Explain Analyse

```js
db.recipes_embedded.find({
  _id: ObjectId("651c00000000000000000201")
}).explain("executionStats")
```

## Metriken

| Metrik              | Altes Modell |      Neues Modell |
| ------------------- | -----------: | ----------------: |
| totalDocsExamined   |            1 |    1 |
| nReturned           |            1 |    1 |
| executionTimeMillis |        46 ms | 0 ms |

## Beweis

![Explain Output neues Modell](./screenshots/phase5-output.png)

## Vergleich

Im alten Modell wurden mehrere Collections über `$lookup` verbunden. Dadurch mussten insgesamt 6 Dokumente untersucht werden und die Ausführung dauerte 18 ms.

Im neuen Modell liegen alle benötigten Rezeptdaten direkt in einem Dokument. Dadurch kann MongoDB das vollständige Rezept mit einer einfachen `find()`-Abfrage laden.

Das Re-Design hat sich gelohnt, weil die Abfrage einfacher ist, keine Joins mehr benötigt und dadurch besser skalierbar ist.